import AuthActionContent from "./_sections/content";

// Khi người dùng nhấn link xác thực sẽ được chuyển đến trang này,
// nên trang này sẽ hiện thị UI để người dùng cập nhật mật khẩu
export default function Page() {
  return <AuthActionContent />;
}
